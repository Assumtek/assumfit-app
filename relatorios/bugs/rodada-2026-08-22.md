
## Em aberto — crash ao abrir (Leonardo, 1.0.5 (3), 22/08 17:33)

**Evidência:** log da Apple (`tf:AFrv2YThCc4nvv6fyh-PSvM`), simbolizado com o dSYM do build 3
(`/private/tmp/assumfit-105b3.xcarchive`): SIGABRT 0,8 s após o lançamento, thread de despacho em fundo,
pilha `ErrorRecovery.crash()` → `runNextTask()` → `notify(newRemoteLoadStatus:)` — o `ErrorRecovery` do
**expo-updates** relançando uma exceção fatal da abertura. A mensagem original (JS fatal ou `NSException`
nativa capturada pelo RN) **não vem no log**.

**O que se sabe:** o código de abertura não mudou entre os builds 2 e 3 (`git diff 4374500..bc14457`), e ele
abria o app antes — o gatilho é ESTADO: sessão de treino aberta desde o crash de "concluir" (15:51Z, o dos hooks)
e pulseira pareada (`connect(pairedDeviceId)` roda na abertura e é o primeiro contato com o SDK ObjC).
O Bruno, sem pulseira, abriu o build 3 normalmente.

**O que o build 4 muda:** o tratador global (`0cf474a`) intercepta erro fatal de JS antes do `RCTFatal`, e a
tela de recuperação (`744f55f`) mostra a mensagem e permite compartilhar — o `/client-errors` só existe depois
do deploy do backend (porta 22).

**Próximo passo:** Leonardo instala a (4); se a tela de recuperação aparecer, o texto compartilhado é o
diagnóstico. Se o app morrer de novo SEM a tela, a exceção é nativa (SDK no `connect`) e o caminho é o
`wireSDKCallbacks()`/`connect` com o aparelho dele.
